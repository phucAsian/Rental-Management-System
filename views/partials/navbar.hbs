<nav class="navbar navbar-expand-lg bg-white border-bottom px-4 py-3">
<div class="d-flex ms-auto">
      {{#if currentUser}} 
        
        {{#if (eq currentUser.role 'Admin')}}
        <a href="/admin/inquiries" class="btn btn-outline-dark me-2 d-flex align-items-center rounded-pill px-3 text-decoration-none">
          <i class="bi bi-bell-fill me-2"></i> Notifications
        </a>
        {{/if}}

        <a href="/profile" class="btn btn-outline-dark me-2 d-flex align-items-center rounded-pill px-3 text-decoration-none">
          <i class="bi bi-person me-2"></i> Profile
        </a>
    
        <a href="/auth/logout" class="btn btn-outline-dark d-flex align-items-center rounded-pill px-3 text-decoration-none">
          <i class="bi bi-box-arrow-right me-2"></i> Logout
        </a>
      {{else}}
        {{/if}}
  </div>
</nav>